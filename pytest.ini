[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = Tests
norecursedirs = Tests/legacy_archive
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
filterwarnings =
    ignore::DeprecationWarning