# SUM Quantum Knowledge OS — Production Dependencies
fastapi>=0.100.0
# Starlette pin (Sprint 2 of the intensification path to arXiv).
# Starlette 1.0.0 removed Router(on_startup=, on_shutdown=) in favour
# of lifespan=. fastapi 0.115.x still passes on_startup=[]/on_shutdown=[]
# defaults to its starlette parent, so fastapi+starlette-1.0 throws
# `Router.__init__() got an unexpected keyword argument 'on_startup'`
# at every APIRouter() construction. Pin until fastapi releases a
# starlette-1.x-compatible version; revisit when the bound below
# stops resolving.
starlette<1.0
uvicorn>=0.49.0
pydantic>=2.0.0
openai>=1.40.0,<3.0.0
httpx>=0.28.1
networkx>=3.4.2
numpy>=1.24.0
scipy>=1.10.0
sympy>=1.12
sse-starlette>=3.4.5
pytest>=7.0.0
pytest-asyncio>=0.21.0
feedparser>=6.0.12
# spacy 3.7.x floor is empirically broken on a fresh venv (F14 — the
# 3.7 wheels' thinc/blis chain fails to resolve under current pip);
# 3.8.0 is the verified-working floor and matches pyproject.toml [sieve].
spacy>=3.8.0
PyJWT>=2.8.0
cryptography>=41.0.0
click>=8.0.0
