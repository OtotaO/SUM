{
  "name": "sum-demo-worker",
  "description": "Cloudflare Worker serving the SUM hosted demo. Static assets + /api/complete + /api/qid.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260810.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "canonicalize": "^4.0.0",
    "jose": "^6.2.8"
  }
}
